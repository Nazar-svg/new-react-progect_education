import React from 'react';
import classes from './Friend.module.css';
const Friends = ({ friends }) => {
// const friendsMaper =  friends.map(friend => <a className={classes.link}>{friend.name}</a>)
        console.log(friends);
    
    return (
        <div>
           {/* {friendsMaper} */}
        </div>
    )
}
export default Friends;